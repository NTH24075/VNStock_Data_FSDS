# =============================================================================
# DataHub — OpenLineage emitter config
# =============================================================================
# Integrated with Airflow + Spark to publish lineage:
#   Bronze → Silver → Gold → Feature tables
#
# Evidence: DataHub UI screenshots (lineage graph, schema metadata, contract assertions)
# placed in docs/images/ and linked from docs/data_governance.md
#
# Based on 02_schema_design.md §7.4
# =============================================================================
